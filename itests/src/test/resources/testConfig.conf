[realm.server]
host=localhost

[world.server]
host=localhost

[jdbc.realmd]
url = jdbc:mysql://localhost/realmd?useSSL=false
username = realmd
password = realmd

[jdbc.objects]
url = jdbc:mysql://localhost/objects?useSSL=false
username = realmd
password = realmd

[jdbc.world]
url = jdbc:mysql://localhost/world?useSSL=false
username = realmd
password = realmd

[jdbc.characters]
url = jdbc:mysql://localhost/characters?useSSL=false
username = realmd
password = realmd